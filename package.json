{
  "name": "taskmanager-fullstack",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npx serve -s frontend/dist -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.6"
  }
}